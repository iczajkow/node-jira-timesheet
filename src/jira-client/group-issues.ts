import { IssueWorklog } from "./models/issue-worklog";
import moment = require("moment");

// export const groupIssues = (issueWorklogs: IssueWorklog[]) => {
//   issueWorklogs.reduce((previous, current) => {
//     const getDate(current.)
//   }, {});
// }

// const getDate = (date: string) => {
//   return moment(date).startOf("day").format("YYYY-MM-DD");
// }
